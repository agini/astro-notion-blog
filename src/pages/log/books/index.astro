---
import Layout from "../../../layouts/Layout.astro";
import { getPostsByTagAndPage } from "../../../lib/notion";

const posts = await getPostsByTagAndPage("book", 1);
---

<Layout title="読書ログ">
  <h1>📚 読書ログ</h1>

  {posts.length === 0 ? (
    <p>まだ読書ログはありません</p>
  ) : (
    <ul>
      {posts.map((post) => (
        <li>
          <a href={`/log/${post.slug}`}>{post.title}</a>
        </li>
      ))}
    </ul>
  )}
</Layout>
