---
import Layout from "../../../layouts/Layout.astro";
import { getPostsByTagAndPage } from "../../../lib/notion";

const posts = await getPostsByTagAndPage("music", 1);
---

<Layout title="音楽ログ">
  <h1>🎵 音楽ログ</h1>

  {posts.length === 0 ? (
    <p>まだ音楽ログはありません</p>
  ) : (
    <ul>
      {posts.map((post) => (
        <li>
          <a href={`/log/${post.slug}`}>{post.title}</a>
        </li>
      ))}
    </ul>
  )}
</Layout>